# Memory Module Alpha Gate

状态：**NOT READY FOR REAL USER DATA**（2026-08-25）

| Gate | 当前状态 | 证据 | 结论 |
| --- | --- | --- | --- |
| tenant/user/relationship/session 隔离 | code+unit+local concurrency/real local acceptance green | `npm test` + `npm run test:companion-core-chat-concurrency` + `npm run test:companion-core-auth` | 主应用并发 chat reservation 防止同一 session 重复写入；required Auth + PostgreSQL 下通过双用户隔离、LifeState 隔离、同用户 CAS 冲突和账户删除传播；云托管环境仍需复验 |
| canonical subject-bound DB constraints | schema+repository+real local SQL green; small and lean 1M pgvector/HNSW acceptance green | `artifacts/companion-core-acceptance-rerun-2026-08-24.json` + `artifacts/memory-postgres-vector-benchmark-1m-lean-2026-08-24.json` + `npm run test:memory-postgres-acceptance` with `MEMORY_MODULE_ACCEPTANCE_PGVECTOR=true` | 本地 PostgreSQL 真实执行通过；HNSW plan 和 lean 1M index-document run 通过；完整 1M canonical assertion/version volume 仍未验收 |
| S3 不落库/不进 outbox/model | code+unit green | ingress/extraction/model tests、fixture scan | 通过已知模式；需生产日志/WAL/proxy sampling |
| read-your-write / S2 confirmation / TTL | code+unit green | domain/API tests | 通过代码基线 |
| candidate/confirm/correct/pin/revoke/forget/delete | code+unit+isolated product HTTP acceptance green | governance tests、V1 API tests、`npm run test:companion-core-memory-lifecycle`、`npm run test:companion-core-chat-edit` | 通过当前代码基线；真实传播计时、缓存/日志/供应商副本语义仍未验收 |
| export snapshot operation | code+unit+API+SDK+real local PostgreSQL/required Auth green | `memory-module.test.js`、`memory-module-api.test.js`、`memory-module-sdk.test.js`、`artifacts/companion-core-acceptance-2026-08-24.json` | Memory export 与主应用 product export operation 均覆盖 subject-bound、fixed commit sequence、stale/expired fail-closed；托管部署仍需复验 |
| worker lease/retry/dead-letter/fencing | code+unit+real local smoke green; two-process crash-takeover and virtual-clock outage/backlog acceptance green | worker/recovery/service-worker tests + `npm run test:memory-postgres` + `npm run test:memory-multiprocess` + `npm run check:companion-outage-backlog` + `artifacts/companion-core-acceptance-rerun-2026-08-24.json` + `artifacts/companion-core-outage-backlog-2026-08-24.json` | 本地 smoke 与双独立进程验收覆盖 claim、单次消费、lease 到期 takeover、stale completion fencing、崩溃接管；虚拟时钟演练验证 `next_attempt_at` 退避和 backlog 不饥饿；生产持续 outage、真实 provider 恢复时间和 RPO/RTO 仍需部署证据 |
| profile/index/episode flags | code+unit green | service worker tests | 通过 wiring；需真实 backlog/freshness |
| proactive mention flag/cooldown | code+unit+API green | `memory-module.test.js` + `memory-module-api.test.js` + `memory-module-schema.test.js` | disabled 时 fail-closed；仅对已授权 Agent 记录 content-free cooldown；真实部署行为仍需验证 |
| versioned ContextBundle cache | code+unit green; optional Redis wiring | `memory-module-cache.test.js` + PostgreSQL repository cache test | 默认关闭；只缓存无 query 的 bounded read model，tenant/user 与 commit/grant/privacy generation 绑定；Redis 故障回退 canonical PostgreSQL |
| hybrid/vector/RRF fallback | code+unit+small real pgvector/HNSW and lean 1M acceptance green; full capacity incomplete | retrieval/domain/native retriever/HTTP adapter tests + `MEMORY_MODULE_ACCEPTANCE_PGVECTOR=true npm run test:memory-postgres-acceptance` + 本地 `artifacts/companion-core-acceptance-rerun-2026-08-24.json`、`artifacts/memory-postgres-vector-benchmark-2026-08-24.json`、`artifacts/memory-postgres-vector-benchmark-1m-lean-2026-08-24.json` | provider/pgvector 不可用会回退 lexical；100k/20 hybrid p50 138.05ms、p95/p99 142.18ms；lean 1M/20 hybrid p50 916.14ms、p95/p99 1173.35ms，20/20 成功；完整 canonical 1M 和生产 SLO 仍未验收 |
| 600-case evaluation | synthetic baseline green; real evaluation missing; input gate fail-closed | v0.2 synthetic JSON + in-memory runner + metrics harness + `server/memory-module-eval.test.js` + `npm run evaluate:memory-synthetic` / `npm run evaluate:memory` | harness 已输出 Recall@5/10、MRR、nDCG、category/Scope、no-answer、conflict、authorization、S2/S3 和 proactive-mention 指标；真实 CLI 现在要求 real/deidentified、`synthetic:false`、版本、provenance、完整 600 cases 和 `split=all`，但真实/脱敏结果 JSON、抽取/晋升/主动提及质量指标仍未提供 |
| PostgreSQL schema/repository | real local lexical + small pgvector acceptance green | `artifacts/companion-core-acceptance-rerun-2026-08-24.json`、`memory-postgres-vector-benchmark-2026-08-24.json`、`npm run test:memory-postgres`、`npm run test:memory-postgres-acceptance` | 本地 PostgreSQL 真实执行覆盖重复 schema、主体/租户隔离、native lexical/vector、Scope、outbox fencing、CAS 和 HNSW plan；1M vector/HNSW 仍未验收 |
| PITR/tombstone replay/RPO/RTO | isolated PITR/tombstone replay and local virtual outage/backlog green; production RPO/RTO missing | `npm run check:companion-backup-restore` + `npm run check:companion-pitr` + `npm run check:companion-outage-backlog` + 本地 `artifacts/companion-core-acceptance-rerun-2026-08-24.json`、`artifacts/companion-core-outage-backlog-2026-08-24.json` | 隔离临时 PostgreSQL 已恢复到删除前时间点并重放 ledger，raw/assertion/index/outbox/negative-read 均通过；虚拟时钟 worker 演练覆盖退避和恢复；生产 RPO/RTO SLO 仍需托管环境证据 |
| 1M-doc/20-concurrent p50/p95/p99 | 1M lexical green; lean 1M pgvector/hybrid green; full canonical 1M remains resource-blocked | `artifacts/memory-postgres-benchmark-2026-08-24.json` + `artifacts/memory-postgres-vector-benchmark-2026-08-24.json` + `artifacts/memory-postgres-vector-benchmark-1m-lean-2026-08-24.json` | lexical 1M/20：p50 39.49ms、p95/p99 368.29ms；vector 100k/20：p50 138.05ms、p95/p99 142.18ms；lean vector 1M/20：p50 916.14ms、p95/p99 1173.35ms、HNSW rebuilt、20/20 成功；完整 canonical 1M 仍需更大受控 volume，生产降级 SLO 仍未执行 |
| Model Gateway retention/quality | Memory extraction/embedding wrappers plus Companion chat gateway, HTTP adapters, unit/fixture audit green; real provider audit missing | gateway wrapper/HTTP adapter/native fallback tests + `server/companion-model-gateway.test.js` + `npm run check:memory-model-gateway` + 本地 `artifacts/memory-model-gateway-audit-2026-08-24.json` + Model Gateway spec | 本地 fixture 已验证 S2/S3 输入门禁、聊天 secret 输入/输出 fail-closed、流式 split-secret fencing、输出过滤、retry/error mapping、content-free telemetry 和 retention metadata；真实 structured extraction 质量、供应商 retention/region/training/deletion SLA 仍未完成 |
| Cochpia/API SDK first external chain | code+unit+temporary local independent PostgreSQL service green; signed service-auth replay smoke green | `npm run test:memory-sdk` + `server/service-auth.test.js` + local signed HTTP smoke | create → retrieve → ContextBundle → export snapshot → forget negative-read 已通过；同 nonce 第二次请求被 PostgreSQL replay ledger 拒绝；正式独立服务网络边界、Redis/TLS/容量仍需复验 |
| production TLS/security headers | code preflight+temporary TLS acceptance green; hosted verification missing | `server/auth.test.js` + `server/db-ssl.test.js` + `server/service-auth.test.js` + `npm run test:companion-core-tls` + security-header tests | 主应用和独立 Memory Module 在 production 下拒绝 auth-off、非 PostgreSQL、未校验证书或 static service-auth 配置；临时 PostgreSQL TLS 集群以 CA 校验通过，`pg_stat_ssl.ssl=true`，正式托管证书链/HTTPS 仍需部署验证 |

## Required final evidence before completion

可运行 `npm run check:companion-alpha-gate` 做只读、fail-closed 的证据审计；它不会连接或写入 PostgreSQL，也不会把 synthetic、lean 或 fixture 结果升级为 Alpha 证据。

1. 配置真实 PostgreSQL，执行 schema、repository smoke、并发冲突和 leased worker test。
2. 在生产类托管环境完成删除前 PITR/backup 与 RPO/RTO 演练；本地隔离 PITR、deletion ledger、retrieve、snapshot、index、outbox 负向验证和虚拟时钟 outage/backlog 演练已通过。
3. 提供带版本和 provenance 的真实/脱敏 600-case cases/results envelope，执行全量 `split=all` 评测，生成 Recall@5/10、MRR、nDCG、no-answer、conflict、Scope、S2/S3 和 mention 报告。
4. 执行 1M index documents、20 concurrent、强 Scope 过滤和积压场景压测，记录 p50/p95/p99 和降级率。
5. 接入正式结构化 Model Gateway，完成模型输入/输出脱敏、保留策略和质量审计。
6. 所有 P0/P1 安全与治理问题关闭或由明确负责人签署风险接受。
