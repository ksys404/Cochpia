# 记忆与性格机制（共生人生 · 地基）

> 本文档说明 Cochpia 已落地的「记忆形成」与「性格形成」两大系统的设计模式与代码运行方式。
> 它们是《共生人生》游戏方案中"规则引擎当大脑、LLM 当嘴"的地基：日常沉淀零成本，人格变化可审计、可回滚。
> 更新日期：2026-08-15

---

## 一、记忆模块：事件、断言、检索与治理

### 模式 1：自动沉淀（聊完自动判断要不要记）

不是每条消息都记，而是启发式判断"值不值得长期记住"。

代码：`server/auto-memory.js`

```js
// 是否值得沉淀为长期记忆
shouldRemember(text) {
  if (text.length < 12) return false;           // 太短不记
  const significant = /(记住|喜欢|讨厌|重要|承诺|约定|生日|想要|害怕|梦想|决定|计划)/.test(text);
  return text.length >= 40 || significant;      // 够长 或 含关键词
}

// 情感坐标：效价(正/负) + 唤醒度(激动程度)
analyzeMessage(text) {
  // 命中正向词 +0.2/个，负向词 -0.2/个 → valence ∈ [-1, 1]
  // 感叹号/问号越多 → arousal 越高
}
```

### 记忆模块的内部链路

代码边界：`server/memory-module.js`、`server/memory-module-api.js`、`server/memory-module-runtime.js`。

```text
聊天/外部调用
  → recordEvent（原始事件）
  → hold / createCandidate（记忆断言）
  → policy / confirmation（敏感性与权限治理）
  → retrieve / contextBundle（检索与上下文预算）
  → correct / revoke / forget（版本与删除）
```

Memory Module 是唯一记忆事实源。旧 `/api` 路径只作为兼容适配层，调用的仍是同一个模块，不再写入旧 `state.memories`。

### 对外接口

稳定接口由 `server/memory-module-sdk.js` 暴露，HTTP 路由由 `server/memory-module-api.js` 提供：

| 能力 | 接口 |
|------|------|
| 记录事件 | `POST /v1/events` |
| 创建/列出记忆 | `POST/GET /v1/memories` |
| 检索 | `POST /v1/retrieve` |
| 构建上下文 | `POST /v1/context-bundles` |
| 确认/修正/撤销 | `/v1/confirmations/*`、`/v1/memories/:id/*` |
| 忘记/删除 | `/v1/governance/*`、`DELETE /v1/memories/:id` |

`/v1` 是接口版本，不是模块名称；模块本身统一称为 Memory Module。

---

## 二、性格形成：一个版本机 + 证据确认制

### 设计：人格是"长"出来的，不是"设置"的

代码：`server/psychology.js` —— 7 个可度量维度，锚定心理学理论：

| 维度 | key | 理论来源 | 基线 |
|------|-----|---------|------|
| 安全感 | security | 依恋理论 | 0.6 |
| 回应性 | responsiveness | 依恋理论 | 0.7 |
| 共同调节 | coregulation | 多迷走神经理论 | 0.6 |
| 激活度 | activation | 多迷走神经理论 | 0.5 |
| 自主支持 | autonomy | 自我决定论 | 0.7 |
| 胜任肯定 | competence | 自我决定论 | 0.6 |
| 归属联结 | relatedness | 自我决定论 | 0.7 |

另有 5 个「互动氛围」预设（安全港湾 / 温柔同行 / 平静调节 / 好奇探索 / 安静守护），通过 system prompt 注入语气。

### 核心机制：变更必须"有证据 + 有版本 + 可回滚"

代码：`server/personality.js`

```js
applyPersonalityChange(personality, history, { evidenceId, proposedChange, action }) {
  // 1. 校验证据和变更（无 traitKey / delta 非法 / delta=0 → 拒绝，返回 null）
  // 2. 找到对应维度，delta 累加（clamp 到 0~1）
  // 3. version + 1，旧版本快照压入 history
  // 4. 返回 { personality: 新人格, history: 新历史 }
}
```

三个铁律：

1. **显式变更** —— 没有证据 ID 的变更一律拒绝。
2. **版本可审计** —— 每次变化 `version+1`，快照进 `personalityHistory`。
3. **可回滚** —— `POST /api/personality/rollback` 回到任意历史版本，并写审计记录。

---

## 三、完整运行链路（从聊天到人格变化）

```
用户发消息
   ↓
POST /api/chat/stream            (server/index.js)
   ↓
① Memory Module.contextBundle(消息内容) ← 检索、策略过滤、预算裁剪
   ↓
② buildRuntimeContext({            ← 组装 LLM 上下文 (server/runtime-context.js)
     messages,                     ← 最近 20 条（去掉被替代的）
     personality,                  ← 人格版本 + traits
     recalled,                     ← Memory Module 返回的记忆
     summary, persona,             ← 会话摘要、人格
     atmosphere, upcomingEvents     ← 氛围、临近事件
   })
   ↓
③ LLM 流式回复（SSE：meta → text → done）
   ↓
④ 回复完成后自动沉淀：
   if (shouldRemember(用户消息)) {
     held = Memory Module.hold(...) ← 存一条记忆断言
     growthEvidence.grow({          ← 生成成长证据（draft）
       claim: 'Cochpia 正在学习把共同经历纳入后续回应。',
       evidence: `本次对话形成事件记忆 ${held.id}`,
       proposedChange: { traitKey: 'warmth', delta: 0.005 }
     })
   }
   ↓
⑤ 前端「成长证据时间线」用户确认/驳回
   ↓
PATCH /api/growth/evidence/:id  {status:'confirmed'}
   ↓
⑥ reviewEvidence() → applyPersonalityChange()
    人格维度真实变化，version+1，写审计
   ↓
⑦ 新人格注入下一次对话的 runtimeContext → 循环
```

---

## 四、API 索引

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/memory/overview` | 记忆概览（数量 + 最近记忆） |
| GET | `/api/personality` | 当前人格（含 evidenceCount） |
| GET | `/api/personality/history` | 人格版本历史 |
| GET | `/api/personality/audit` | 变更审计日志 |
| GET | `/api/growth/evidence` | 成长证据列表（支持 `?status=` `?search=` `?paginated=true`） |
| GET | `/api/growth/evidence/:id` | 单条证据 |
| PATCH | `/api/growth/evidence/:id` | 审核单条（draft/confirmed/rejected，confirmed 时应用人格变更） |
| POST | `/api/growth/evidence/batch` | 批量审核（1~100 条） |
| POST | `/api/personality/rollback` | 回滚到指定版本 |

---

## 五、关键代码文件索引

| 文件 | 职责 |
|------|------|
| `server/auto-memory.js` | 启发式记忆判定 `shouldRemember` + 情感分析 `analyzeMessage` |
| `server/memory-module.js` | 记忆断言、事件、检索、治理和删除 |
| `server/memory-module-runtime.js` | 应用接入、上下文绑定和旧数据一次性迁移 |
| `server/memory-module-api.js` | 对外 HTTP 接口适配 |
| `server/memory-module-sdk.js` | 外部调用 SDK |
| `server/growth-evidence.js` | 独立的人成长证据与确认状态 |
| `server/personality.js` | 人格变更 `applyPersonalityChange` + 回滚审计 |
| `server/psychology.js` | 7 心理学维度 + 氛围预设 + 幂等补齐 |
| `server/runtime-context.js` | 组装注入 LLM 的运行时上下文 |
| `server/index.js` | 路由 + 聊天流 + 记忆/证据/人格接口 |
| `client/src/main.jsx` | 前端聊天 + 成长证据时间线（确认/驳回/批量） |

---

## 六、一句话总结

> **记忆**：聊天事件进入 Memory Module → 形成受治理的记忆断言 → 按策略检索并构建上下文。
> **性格**：聊天只"提出证据"（draft），用户"确认"才真正改变人格，每次变化都升版本、留快照、可回滚。
> **闭环**：`聊天 → 记忆 + 证据 → 用户确认 → 人格版本+1 → 新人格影响下一次聊天`。

---

## 七、给非技术同学的白话版

- **记忆**：像人会"记住重要的、忘掉不重要的"。聊天事件先进入 Memory Module，再经过敏感性、权限和确认策略，最后才进入可召回上下文。
- **性格**：Cochpia 不会"突然变"。每次聊天它只**悄悄写一条"成长小纸条"**（证据），你点了"确认采纳"，它才真的朝那个方向长一点，并且每次变化都留了"历史快照"，随时能退回之前的版本。
- **为什么这么设计**：防止 AI 人格被一次对话带偏，也让用户始终能"看得到、管得住"它的成长，符合产品"可验证成长"的定位。
