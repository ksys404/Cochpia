# 安全与隐私处理规范(必读)

本项目(Cochpia)对安全性与隐私有严格要求。所有二次开发、代码修改与提交必须遵守以下规则,严格程度对齐 Codex 的安全隐私处理标准。

## API Key 与密钥

- 任何 API key、token、secret 严禁写入源代码、配置文件、示例文件、注释或提交历史。
- 真实密钥只允许存放在 `.env`(该文件已被 `.gitignore` 忽略),禁止 `git add .env` 或以任何形式提交。
- `.env.example` 只能放占位符(如 `your-...`、空值或注释说明),禁止写入真实密钥。
- 禁止在日志、终端输出或对话回复中打印完整密钥;展示时必须脱敏(如只显示前几位或标记为 `redacted`)。

## 提交前检查

- 每次提交前扫描工作区,确认没有硬编码密钥:

  ```
  grep -rInE "sk-[A-Za-z0-9]{10,}|api[_-]?key\s*[:=]\s*['\"]?[A-Za-z0-9]{12,}" \
    --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=.git --exclude=.env .
  ```

- 检查 `git status` 与 `git diff --cached`,确认 `.env` 及任何密钥文件未被追踪或暂存。
- 若历史中误提交过密钥,必须立即轮换(revoke/rotate)密钥并清理历史,而不是仅删除文件。

## 其他隐私要求

- 不把用户个人信息、对话内容、运行数据写入仓库。
- 凭据遵循最小权限、最短有效期原则,优先通过环境变量注入。

## 市场化与生产标准(必读)

本项目定位为面向市场的商业产品,处理真实用户的个人与对话数据,受《个人信息保护法》《数据安全法》《生成式人工智能服务管理暂行办法》等约束。所有开发必须以生产级标准对待,上线前以下硬性要求必须全部满足:

- 鉴权与存储:`AUTH_MODE=required` + `STORAGE_PROVIDER=postgres`(代码已有启动 guard,但必须实际配置并验证)。
- 全站 HTTPS + 安全响应头:CSP、X-Frame-Options、X-Content-Type-Options、Referrer-Policy。
- 数据库连接必须做 TLS 证书校验(修复当前 `rejectUnauthorized: false`)。
- 修复 Gemini 密钥经 URL query 传递的泄露点,改为请求头。
- 日志与监控必须脱敏,不得记录用户对话正文、密钥、DATABASE_URL。
- 提供数据主体权利能力:用户可查看、导出、删除自己的记忆与数据。
- 依赖漏洞扫描、密钥轮换、备份与恢复演练。

开发纪律:

- 每次改动前先做安全与隐私影响评估,涉及个人数据的功能必须可审计、可回滚。
- 提交前跑测试 + 硬编码密钥扫描(见上文),并确认未引入生产配置或密钥。
- 对外发布前需逐项核对本节的"硬性要求",未达标不得上线。

## 当前基线

- 密钥位于项目根目录 `.env`(如 `MODEL_DEEPSEEK_API_KEY`),已 gitignore 且未被 git 追踪。
- 仓库目前暂无 commit,不存在历史泄露风险。
